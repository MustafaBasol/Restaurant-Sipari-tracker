# Stripe backend sample env (used by server.cjs)
# Do NOT commit real secrets. Use Codespaces/CI secrets or local .env.

# Required
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID_MONTHLY=price_...

# Recommended (required for processing Stripe webhooks securely)
# STRIPE_WEBHOOK_SECRET=whsec_...

# CORS: use ORIGIN only (no hash, no path)
# Local:
# CORS_ORIGINS=http://localhost:3000
# Codespaces:
# CORS_ORIGINS=https://<codespace>-3000.app.github.dev
# Or allow any Codespaces origin (demo-only):
# CORS_ORIGINS=https://*.app.github.dev
CORS_ORIGINS=http://localhost:3000

# Backend port (default 4242)
PORT=4242

# Optional: Protect API endpoints with an API key (webhook excluded)
# If set, clients must send `x-api-key: <API_KEY>`
# API_KEY=change-me

