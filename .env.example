# Stripe backend sample env (used by server.cjs)
# Do NOT commit real secrets. Use Codespaces/CI secrets or local .env.

# Required
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PRICE_ID_MONTHLY=price_...

# CORS: use ORIGIN only (no hash, no path)
# Local:
# CORS_ORIGINS=http://localhost:3000
# Codespaces:
# CORS_ORIGINS=https://<codespace>-3000.app.github.dev
CORS_ORIGINS=http://localhost:3000

# Backend port (default 4242)
PORT=4242

# Optional (only needed if you configure Stripe webhooks)
# STRIPE_WEBHOOK_SECRET=whsec_...
