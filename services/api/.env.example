# Core API (services/api)
# Do NOT commit real secrets. Use local .env or CI secrets.

# Required
DATABASE_URL=postgresql://kitchorify:change-me@localhost:5432/kitchorify
CORS_ORIGINS=http://localhost:3000

# Optional
PORT=4000
NODE_ENV=development
SESSION_TTL_DAYS=30

# --- Optional: MFA (TOTP) ---
# Issuer name shown in authenticator apps.
MFA_ISSUER=Kitchorify

# --- Email (MailerSend) ---
# Set MAILERSEND_ENABLED=true to send email verification and password reset emails.
MAILERSEND_ENABLED=false
MAILERSEND_API_KEY=mlsn_...
MAILERSEND_FROM_EMAIL=no-reply@example.com
MAILERSEND_FROM_NAME=Kitchorify

# Public URL of the frontend used for email links.
# For hash-router, links are built like: ${APP_PUBLIC_URL}/#/verify-email?token=...
APP_PUBLIC_URL=http://localhost:3000

# Token TTLs (minutes)
EMAIL_VERIFICATION_TTL_MINUTES=60
PASSWORD_RESET_TTL_MINUTES=30

# --- Optional: Human verification (Turnstile) ---
TURNSTILE_ENABLED=false
TURNSTILE_SECRET_KEY=0x4AAAAAA...
