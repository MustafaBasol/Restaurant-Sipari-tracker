# Backup sync (off-host) environment example
# Copy to your VPS as: /etc/kitchorify/backup-sync.env  (DO NOT COMMIT REAL VALUES)

# Absolute path to deploy/backups on the VPS
# Example (if repo lives at /opt/Restaurant-Sipari-tracker):
# BACKUPS_DIR=/opt/Restaurant-Sipari-tracker/deploy/backups
BACKUPS_DIR=/opt/Restaurant-Sipari-tracker/deploy/backups

# rclone config file path on the VPS
# Create it with: docker run --rm -it -v /etc/kitchorify:/config rclone/rclone config
RCLONE_CONFIG=/etc/kitchorify/rclone.conf

# rclone remote name (as defined in rclone.conf)
RCLONE_REMOTE=myremote

# Destination path inside the remote (bucket/folder)
# Examples:
# - S3: kitchorify-backups
# - S3 with prefix: kitchorify-backups/prod
RCLONE_DEST=kitchorify-backups/prod

# Optional extra flags (space separated)
# Examples: --s3-storage-class STANDARD_IA --transfers 4 --checkers 8
RCLONE_FLAGS=
