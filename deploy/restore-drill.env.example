# Restore drill (automated DR test) environment example
# Copy to your VPS as: /etc/kitchorify/restore-drill.env  (DO NOT COMMIT REAL VALUES)

# Directory where docker-compose.yml lives on the VPS
DRILL_DEPLOY_DIR=/opt/Restaurant-Sipari-tracker/deploy

# Env file used by docker compose
DRILL_ENV_FILE=/opt/Restaurant-Sipari-tracker/deploy/.env.production

# Backups directory (used to pick the latest backup)
DRILL_BACKUPS_DIR=/opt/Restaurant-Sipari-tracker/deploy/backups

# Optional: only consider files matching this glob (space-separated patterns supported)
# Examples: "*.sql.gz" or "*.dump *.backup"
DRILL_BACKUP_GLOB=*.sql.gz
