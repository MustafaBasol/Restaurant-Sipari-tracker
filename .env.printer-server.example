# Print server sample env (used by printer-server.cjs)
# Do NOT commit real secrets. Use local .env or platform secrets.

# CORS allowlist (use ORIGIN only; no hash, no path)
CORS_ORIGINS=http://localhost:3000

# Server port (default 4243)
PORT=4243

# Optional: Protect API endpoints with an API key
# If set, clients must send `x-api-key: <API_KEY>`
# API_KEY=change-me

# Transport: stdout | tcp9100 | cups
PRINT_TRANSPORT=stdout

# tcp9100 only
# PRINTER_HOST=192.168.1.50
# PRINTER_PORT=9100

# cups only (requires lp)
# PRINTER_NAME=EPSON_TM_T20
